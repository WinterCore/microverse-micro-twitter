<section class="section">
  <div class="container">
    <div class="columns">
      <div class="column is-one-quarter">
        <% if user_signed_in? %>
        <%= render 'profile' %>
        <% end %>
        <%= render 'trends' %>
      </div>
      <%= render 'feed', tweets: @tweets %>
      <%= render 'who_to_follow' %>
    </div>
  </div>
</section>
